# Dependencies
node_modules/

# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log
firestore-debug.log
ui-debug.log
pubsub-debug.log
firebase-export-*/

# Environment variables
.env*
!.env.example

# Build
web/dist/
functions/lib/

# E2E (Playwright) — browsers install to the OS-level cache by default,
# never inside this repo; only these in-repo artifact dirs need ignoring.
test-results/
playwright-report/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# Claude Code (local, per-machine)
.claude/settings.local.json

# Local context checkpoint (regenerated per sub-stage gate, never committed)
CURRENT_SPRINT.md

# local milestone-audit tooling (not versioned)
.claude/skills/milestone-audit/
