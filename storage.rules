rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // No path is implemented yet (M0).
    // Per-path rules are added as the milestones progress, per SRS §6.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
