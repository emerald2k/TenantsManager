# Generated / installed — we do not format these.
node_modules/
web/dist/
functions/lib/
package-lock.json
web/package-lock.json
functions/package-lock.json

# Firebase
.firebase/
*-debug.log

# Firebase Security Rules — their own syntax, Prettier does not understand it.
firestore.rules
storage.rules

# Reference documents written by a human. Prettier governs the CODE, not the
# specification: automatically reformatting the SRS would make its diffs
# unreadable and would hide real requirement changes among blank-line moves.
SRS.md
CLAUDE.md
