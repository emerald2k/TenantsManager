{
  "name": "functions",
  "description": "Cloud Functions for TenantsManager",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "set-admin": "node scripts/setAdminClaim.js",
    "seed": "node scripts/seed.js",
    "test": "vitest run",
    "test:emulator": "firebase emulators:exec --only auth,firestore --project tenants-manager-2026 \"vitest run\""
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
